fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# ML stack — pinned to the project's working .venv (Python 3.11.9).
# Verified via `D:/Documents/player-replacement/.venv/Scripts/python.exe -c "import numpy, scipy, gensim; ..."`
# gensim 4.4 supports NumPy 2.x, so no numpy<2.0 ceiling is needed.
numpy==2.4.4
scipy==1.17.1
gensim==4.4.0

# Test deps.
# httpx is required by FastAPI's TestClient (Starlette TestClient has used
# httpx as its transport since FastAPI 0.87); without it,
# `from fastapi.testclient import TestClient` raises at import time.
pytest>=8.0
httpx>=0.27
