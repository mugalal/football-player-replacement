# Backend base URL. In production (Vercel), set this to your Hugging Face Space URL.
# In local docker-compose, the value here is overridden by the compose `environment:` block.
NEXT_PUBLIC_API_URL=http://localhost:8000
